# website
Odd1Out's home on the web

## Deploy

```bash
git push hetzner stage   # builds new image, deploys to stage.odd1out.band
git push hetzner prod    # promotes stage image to odd1out.band without rebuilding
```
